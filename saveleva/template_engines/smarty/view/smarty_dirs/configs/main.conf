# Глобальные переменные шаблонов
pageTitle = "Some Page Title"
bodyBgColor = "red"
# Секция Customer
[Customer]
pageTitle = "List of Posts"
Intro = """Это значение состоит из нескольких строк,
            поэтому его нужно заключить в тройный кавычки"""
# Скрытая секция Database (убрать точку чтобы открыть)
[.Database]
host = "localhost"
db = "template_engine"
user = "root"
pass = "123"
